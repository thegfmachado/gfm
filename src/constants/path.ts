export const routesPath = {
  home: (locale: string) => `/${locale}`,
  experiences: (locale: string) => `/${locale}/experiences`,
  projects: (locale: string) => `/${locale}/projects`,
  feedbacks: (locale: string) => `/${locale}/feedbacks`,
};
